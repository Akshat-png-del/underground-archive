/**
 * @deprecated Use `playerController` from `@/lib/music/player-controller` instead.
 * Thin legacy alias — all calls delegate to MediaEngine via playback-actions.
 */
export { playerController as player } from "@/lib/music/player-controller";
